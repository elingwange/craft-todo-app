# .github/workflows/ci.yml
jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      # ... 其他步骤

      # 步骤4: 运行测试
      # 使用 `test:ci` 脚本，它会运行一次测试并退出
      - name: Run tests
        run: npm run test:ci
      # ... 其他步骤
